import { Container as defaultContainer } from 'react-bootstrap'
import { styled } from 'styled-components'

export const Container = styled(defaultContainer)`
    margin: 20px auto;
`

export const Wrapper = styled(defaultContainer)`
`